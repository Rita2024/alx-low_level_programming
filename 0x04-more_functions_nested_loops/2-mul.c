#include "main.h"
/**
 * mul - multiplication of two integers
 * @a: the first integer to be multiplied
 * @b: second integer to be multiplied
 * Return: the result of the multiplication between parameter a and parameter b
 */

int mul(int a, int b)
{
	return (a * b);
}
